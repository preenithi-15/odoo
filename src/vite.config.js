import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/Travel/', // Required for GitHub Pages deployment to the /Travel/ repository
})
